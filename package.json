{
  "name": "dental",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "start": "vite preview --host 0.0.0.0 --port $PORT"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  }
}
